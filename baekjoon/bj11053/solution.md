```
dp[n][m], 1 <= n(수열 크기) <= 1_000, 1 <= m(원소 크기) <= 1_000
수열 크기 i에서 만들 수 있는 "가장 긴 증가하는 부분 수열"을 dp[i]에 기록해 놓는다.
그러기 위해서 dp[i-1]에서 seq[i] 인덱스 이하의 값들 중 최댓값 M을 골라 dp[i][Ai] = dp[i-1][M] + 1을 하여 갱신한다.
```
